{
  "private": true,
  "scripts": {
    "start": "ts-node --transpile-only ./index.ts",
    "generate": "prisma2 generate",
    "seed": "ts-node --transpile-only ./seed.ts"
  },
  "dependencies": {
    "apollo-server": "^2.9.12",
    "dataloader": "^2.0.0",
    "graphql": "^14.5.8",
    "reflect-metadata": "^0.1.13",
    "type-graphql": "0.18.0-beta.5"
  },
  "devDependencies": {
    "prisma2": "^2.0.0-preview016.2",
    "ts-node": "^8.5.2",
    "typescript": "^3.7.2"
  }
}
