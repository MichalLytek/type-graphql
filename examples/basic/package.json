{
  "private": true,
  "scripts": {
    "start": "ts-node --transpile-only ./index.ts",
    "dev": "ts-node --transpile-only ./dev.ts",
    "generate": "prisma2 generate",
    "seed": "ts-node --transpile-only ./seed.ts"
  },
  "dependencies": {
    "apollo-server": "^2.9.2",
    "graphql": "^14.5.4",
    "reflect-metadata": "^0.1.13",
    "type-graphql": "^0.17.5"
  },
  "devDependencies": {
    "prisma2": "^2.0.0-alpha.161",
    "ts-node": "^8.3.0",
    "typescript": "^3.6.2"
  }
}
