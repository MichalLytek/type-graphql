{
  "private": true,
  "scripts": {
    "prebuild": "npm run --prefix ../ build && npm run clean",
    "build": "npx tsc --build ./tsconfig.json ./tsconfig.esm.json",
    "postbuild": "npx ts-node ../scripts/package.json.ts --on examples",
    "clean": "npx shx rm -rf ./build"
  },
  "dependencies": {
    "@apollo/cache-control-types": "^1.0.2",
    "@apollo/server": "^4.6.0",
    "@apollo/server-plugin-response-cache": "^4.1.2",
    "joiful": "^3.0.2",
    "pg": "^8.10.0",
    "tsyringe": "^4.7.0",
    "type-graphql": "file:..",
    "typeorm": "^0.3.14"
  }
}
