{
  "private": true,
  "scripts": {
    "build": "npx tsc --build ./tsconfig.json",
    "check": "npx tsc --project ./tsconfig.json --noEmit"
  },
  "dependencies": {
    "graphql": "^16.6.0",
    "graphql-tag": "^2.12.6",
    "reflect-metadata": "^0.1.13",
    "type-graphql": "file:../"
  }
}
